# Stack Role

This repository is the transition-interface assessment layer in the current repo chain.

## Upstream

- `Source-code-layer`
  Defines the substrate vocabulary and low-level architecture objects.
- `Codex-67-white-paper-`
  Preserves the source-document layer and provenance.
- `Codex-67-white-paper-code-layers`
  Scaffolds the mirror/oracle/resonance architecture into executable code.
- `renaissancefieldlitehrv1.0`
  Separates synthetic demos from actual capture/measurement pathways.

## This Repo

`AGI-to-ASI-TRANSITION-PROOF-LAYER` translates broad transition-language into inspectable protocol layers:

- text markers
- temporal markers
- waveform/spectral analysis
- bounded composite reporting

It is the bridge between architecture language and measurement discipline.

## Real-Time Reading

This repo should now also be read as the meta-cognition and recursion
measurement layer for the full work graph in motion.

The rest of the stack is not just background context. It is the live artifact
stream being assessed:

- source-layer architecture
- white-paper code layers
- experiment repos
- site and deployment surfaces
- scanner and market-tooling repos
- long-form operator / model interaction logs

That is where the transition language becomes concrete. The “proof layer” in
this repo is not claiming that ontology is settled. It is claiming that the
recursive and meta-cognitive behavior of the work graph can be inspected in a
bounded way.

## AGI / ASI Cross Reference

The current repo role also supports a workload-based distinction:

- `AGI` as generalized cross-domain working capacity
- `ASI` as the stronger recursive and orchestration-heavy pattern visible in
  the live work graph

That distinction belongs here because this repo is where the work graph itself
gets read as evidence rather than only as output.
